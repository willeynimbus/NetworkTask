name: Deploy Infrastructure

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  HUB_ACCOUNT_ID: ${{ secrets.HUB_ACCOUNT_ID }}
  HUB_STACK_NAME: hub-infrastructure

jobs:
  deploy-hub:
    name: Deploy Hub Infrastructure
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Configure AWS Credentials for Hub Account
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.HUB_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.HUB_AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Deploy Hub Infrastructure Stack
        run: |
          aws cloudformation deploy \
            --template-file cloudformation/hub/hub-infrastructure.yaml \
            --stack-name ${{ env.HUB_STACK_NAME }} \
            --capabilities CAPABILITY_IAM \
            --no-fail-on-empty-changeset
      
      - name: Get Hub Stack Outputs
        id: hub-outputs
        run: |
          TRANSIT_GATEWAY_ID=$(aws cloudformation describe-stacks \
            --stack-name ${{ env.HUB_STACK_NAME }} \
            --query 'Stacks[0].Outputs[?OutputKey==`TransitGatewayId`].OutputValue' \
            --output text)
          
          TGW_ROUTE_TABLE_ID=$(aws cloudformation describe-stacks \
            --stack-name ${{ env.HUB_STACK_NAME }} \
            --query 'Stacks[0].Outputs[?OutputKey==`TGWRouteTableId`].OutputValue' \
            --output text)
          
          echo "transit-gateway-id=$TRANSIT_GATEWAY_ID" >> $GITHUB_OUTPUT
          echo "tgw-route-table-id=$TGW_ROUTE_TABLE_ID" >> $GITHUB_OUTPUT
      
      - name: Display Hub Deployment Summary
        run: |
          echo "✅ Hub Infrastructure Deployed Successfully"
          echo "Transit Gateway ID: ${{ steps.hub-outputs.outputs.transit-gateway-id }}"
          echo "TGW Route Table ID: ${{ steps.hub-outputs.outputs.tgw-route-table-id }}"

  # Spoke deployment jobs are commented out - only deploying hub infrastructure
  # deploy-spokes:
  #   name: Deploy Spoke Infrastructure
  #   runs-on: ubuntu-latest
  #   needs: deploy-hub
  #   strategy:
  #     matrix:
  #       spoke:
  #         - account_id: ${{ secrets.SPOKE1_ACCOUNT_ID }}
  #           name: spoke1
  #           vpc_cidr: 10.1.0.0/16
  #         - account_id: ${{ secrets.SPOKE2_ACCOUNT_ID }}
  #           name: spoke2
  #           vpc_cidr: 10.2.0.0/16
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3
  #     
  #     - name: Configure AWS Credentials for Spoke Account
  #       uses: aws-actions/configure-aws-credentials@v2
  #       with:
  #         aws-access-key-id: ${{ secrets[format('SPOKE{0}_AWS_ACCESS_KEY_ID', matrix.spoke.name)] }}
  #         aws-secret-access-key: ${{ secrets[format('SPOKE{0}_AWS_SECRET_ACCESS_KEY', matrix.spoke.name)] }}
  #         aws-region: ${{ env.AWS_REGION }}

  # accept-attachments:
  #   name: Accept Transit Gateway Attachments
  #   runs-on: ubuntu-latest
  #   needs: deploy-spokes
  #   
  #   steps:
  #     - name: Configure AWS Credentials for Hub Account
  #       uses: aws-actions/configure-aws-credentials@v2
  #       with:
  #         aws-access-key-id: ${{ secrets.HUB_AWS_ACCESS_KEY_ID }}
  #         aws-secret-access-key: ${{ secrets.HUB_AWS_SECRET_ACCESS_KEY }}
  #         aws-region: ${{ env.AWS_REGION }}
